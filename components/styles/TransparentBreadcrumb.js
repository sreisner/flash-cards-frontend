import Breadcrumb from 'react-bootstrap/Breadcrumb';
import styled from 'styled-components';

const TransparentBreadcrumb = styled(Breadcrumb)`
  .breadcrumb {
    background: transparent;
    margin-bottom: 0;
  }
`;

export default TransparentBreadcrumb;
